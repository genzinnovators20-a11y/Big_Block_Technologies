/**
 * Content-aware cards.
 *
 * Each one takes a record from `src/data` and renders it. Pages compose these
 * rather than laying out card internals themselves, so a change to the card
 * language lands everywhere at once.
 */
export { BlogCard } from './BlogCard';
export { CaseStudyCard } from './CaseStudyCard';
export { IndustryCard } from './IndustryCard';
export { NumberedCard } from './NumberedCard';
export { ProcessTimeline } from './ProcessTimeline';
export { ServiceCard } from './ServiceCard';
export { SolutionCard } from './SolutionCard';
export { TechGroupCard } from './TechGroupCard';
